# 数据表设计系统

## 概况

支持 postgreSQL 数据库的设计系统。支持在本地运行，通过连接本地的数据库执行创建动作，用户可以配置不同字段添加类型设置枚举值等操作，在完成字段定义后，生成对应的 postgreSQL 语句，最终执行写入数据的数据库完成创建


## 需求

1. 针对 postgreSQL 数据库
2. 本地运行
3. 支持用户自定义字段，保存生成 SQL 脚本
4. 支持查看不同表之间的关系（外键连接）

## 技术选型

- React
- Typescript
- Antd Design
- postgreSQL
- Vite